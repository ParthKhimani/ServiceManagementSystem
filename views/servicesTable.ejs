<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        img {
            width: 100%;
            height: 100%;
        }

        td {
            width: 15%; 
        }
    </style>
</head>

<body>
    <center>
        <% if(role==1) { %>
            <h1><u>Admin Page</u></h1>
            <% } %>
                <h3>Services List</h3>
                <table style="width: 75%;" class="table table-striped">
                    <thead>
                        <td>
                            Services assigned to or by you
                        </td>
                    </thead>
                    <tbody>
                        <% for(let i=0; i<data.length; i++) { %>
                            <tr>
                                <td>
                                    <%= data[i].services %>
                                </td>
                                <% if(role==1) { %>
                                    <td>
                                        <form action="/deleteServices" method="post">
                                            <input type="hidden" name="delete" value="<%= data[i].services %>">
                                            <input type="hidden" name="mailId" value="<%= mailId %>">
                                            <button type="submit">Delete</button>
                                        </form>
                                    </td>
                                    <% } %>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
                <% if(role==1) { %>
                    <form action="/addServices" method="post">
                        <input type="hidden" value="<%= mailId %>" name="mailId">
                        <button type="submit">Add Services</button>
                    </form>
                    <% } %>
                        <br>
                        <form action="/logout" method="post">
                            <button type="submit">Log Out</button>
                        </form>
    </center>
</body>

</html>